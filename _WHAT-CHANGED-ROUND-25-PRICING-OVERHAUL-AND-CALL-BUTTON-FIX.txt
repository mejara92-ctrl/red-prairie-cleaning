RED PRAIRIE — ROUND 25: DEPOSIT-MATH PRICING OVERHAUL, NEW ADD-ONS,
SCOPE TABLE FIXES, SITE-WIDE CALL BUTTON FIX
====================================================================
This zip contains ONLY the files that changed. Unzip it over your repo
root and let it overwrite matching paths.

Your instructions, across several messages:
  1. "Okay make all reccommended changes to pricing. An inspection
     move-out should cost alot. A quick move-out should be fair but a
     good value. Make all reccomendaed changes to make this the best
     business get alot of sales and make some money."
  2. "each addon should have a price" (with a screenshot of the scope
     table showing "–" where oven/cabinets/baseboards/windows/walls
     should show real numbers)
  3. "The addons on move-out refresh arent showing besides the oven."
  4. "Create competitive but profitable pricing for lawton based on the
     deposit math sheet."
  5. Mid-session: "call button is broken, doesn't mention ceiling fans,
     vents, light fixtures, make the list cover all the big stuff."

Five things happened this round. They're unrelated to each other except
that #5 landed in the middle of building #1-4, so it's folded in here
instead of shipping as its own round.


FILES CHANGED (4)
  js/rp-pricing-engine.js   modified
  book/index.html           modified
  call/index.html           modified
  pricing/index.html        modified
  + 16 other pages          modified (Call button fix only — see #5)


--------------------------------------------------------
1. BOTH MOVE-OUT TIERS RE-PRICED FROM THE DEPOSIT MATH SHEET
--------------------------------------------------------
The core problem, from the feedback pass before this round: Inspection
Ready's live price was never raised to fund its own 8% guarantee
reserve when the guarantee was added. It was carrying the same
~79%-labor-share problem the whole Deposit Math redesign existed to
fix — the guarantee that's supposed to be the tier's selling point was
quietly unfunded.

Formula (from your worksheet, unchanged):
  labor(hrs) = hrs * 2 crew * $17.50/hr * 1.12 burden
  cost       = labor + $15 supplies + $25 overhead
  Inspection Ready only: cost = cost / (1 - 8% guarantee reserve)
  price      = cost / (1 - 20% target margin)

Worksheet hours: Inspection Ready 6/8/10/12 hrs by size, Refresh
3/4/5/6 hrs by size (unchanged from before).

                    Old price   New price   Formula target
Inspection Ready
  1-2 bed             $299        $379        $373.91
  3 bed               $399        $479        $480.43
  4 bed               $499        $589        $586.96
  5+ bed              $599        $699        $693.48
Move-Out Refresh
  1-2 bed             $179        $199        $197
  3 bed               $229        $249        $246
  4 bed               $279        $299        $295
  5+ bed              $329        $349        $344

Rounded to clean $10s above the formula's exact number in every case —
never below it, so the reserve and margin are always at least fully
funded, usually with a couple dollars of headroom. Inspection Ready
moved up 21-27% depending on size; Refresh moved up 6-11%. Per your
framing: Inspection Ready is priced to actually cost a lot (it backs a
real guarantee and a full-scope job), Refresh stays the fair, good-value
option for people with no walkthrough to pass.

Nothing about hours, crew size, or scope changed — same 2-cleaner crew,
same time on site, same guarantee split (Defend Your Deposit on
Inspection Ready, Satisfaction Guaranteed on Refresh). This is strictly
a "the number was wrong for what it has to fund" fix.


--------------------------------------------------------
2. THREE NEW PRICED ADD-ONS ON MOVE-OUT REFRESH
--------------------------------------------------------
Refresh could only add the refrigerator interior. Every other
Inspection-Ready-only item was a hard "no" that pushed the customer to
switch tiers entirely — which is fine if that's what they want, but
someone who's 90% happy with Refresh and just wants the oven done had
no way to buy that.

New, alongside the existing Refrigerator Interior ($50):
  Oven Interior              $50
  Inside Cabinets & Closets  $50
  Detail Pass                $75 / $125 / $175 / $225 by bedroom count
    (baseboards & trim, interior windows/sills/tracks, wall
    spot-cleaning, and — per your note — ceiling fans, vents & light
    fixtures, all bundled as one purchase, not four separate line
    items to tap through)

Detail Pass is priced by bedroom tier (same brackets as the tiers
themselves) rather than a flat number, since a 5-bedroom home has a lot
more baseboard and window to detail than a 1-bedroom. It's one SKU, not
three or four, on purpose — round 21 established "keep booking dead
simple" as a standing principle, and a customer already knows what a
"Detail Pass" costs without pricing baseboards, windows, walls, and
fans/vents/lights separately.

Pricing psychology check (same standard applied everywhere else on
this site): buying every excluded-scope add-on on Refresh must always
cost strictly more than just switching to Inspection Ready outright.
Verified at all four sizes — buying fridge + oven + cabinets + Detail
Pass costs $25-$45 MORE than the tier-price gap at every size, so
switching tiers is always the better deal for someone who wants
everything, and a la carte stays the right call only for someone who
genuinely wants just one or two items.

  1-2BR: $50+$50+$50+$75  = $225  vs. tier gap $379-$199 = $180  (+$45)
  3BR:   $50+$50+$50+$125 = $275  vs. tier gap $479-$249 = $230  (+$45)
  4BR:   $50+$50+$50+$175 = $325  vs. tier gap $589-$299 = $290  (+$35)
  5+BR:  $50+$50+$50+$225 = $375  vs. tier gap $699-$349 = $350  (+$25)

Applied everywhere an add-on has to show up consistently: /book's
add-ons screen (new cards, same Yes/No pattern as Refrigerator
Interior), /call's addon picker (new checkboxes for Liz) and its
price-objection script (removableAddonLabel — Liz can now offer to
drop any of the three off the total, not just fridge/carpet/junk/etc.),
the webhook (new addon_oven_interior / addon_cabinets_closets /
addon_detail_pass fields), and the crew sheet / job details (all read
the shared rpAddonLineItems() function, so nothing extra had to be
built there).


--------------------------------------------------------
3. SCOPE TABLE — EVERY ROW NOW SHOWS A REAL PRICE
--------------------------------------------------------
You flagged this with a screenshot: the Inspection-Ready-vs-Refresh
comparison table on /book showed a bare "–" for oven, cabinets,
baseboards, interior windows, and wall spot-cleaning — everywhere the
refrigerator row showed "+$50," the rest showed nothing, which read as
"you cannot get this at all on Refresh" even before today's new
add-ons existed to buy them with.

Fixed on both /book's customer-facing table and /call's CSR "scope
cheat sheet" (the console's own copy of the same comparison, added in
round 22):
  - Oven, refrigerator, and cabinets & closets each show their real
    $50 add-on price instead of a dash.
  - Baseboards, interior windows, and wall spot-cleaning — previously
    three separate rows all showing "–" — are now ONE row ("Baseboards,
    interior windows, walls, ceiling fans, vents & light fixtures")
    showing the real Detail Pass price for that size, matching the one
    SKU it actually is.
  - Per your note that the list should "cover all the big stuff" and
    call out ceiling fans, vents, and light fixtures by name: those are
    now explicitly named in that combined row, in the Detail Pass's own
    description text on the add-ons screen, in Inspection Ready's full
    item list, and in Refresh's exclusion fine print — not just
    silently folded into "baseboards" the way they used to be.

The `mark()` helper on both pages went from a hardcoded "always show
the fridge price for any addon-eligible row" to reading the real price
per row, so this can't drift out of sync with the catalog again.


--------------------------------------------------------
4. COPY THAT WAS QUIETLY WRONG, FIXED WHILE TOUCHING THIS AREA
--------------------------------------------------------
A few spots described the OLD Refresh scope (nothing purchasable but
the fridge) and would have actively undersold the new add-ons if left
alone:
  - Move-Out Refresh's fine print (shown on /book's tier cards, /call's
    rail, and read by the engine everywhere rpIncludes.moveoutrefresh
    is used) said "Fridge interior can be added individually... for
    the rest, Inspection Ready covers all of it" — now lists all three
    new add-ons by name and price range instead of routing customers to
    switch tiers for things they can now just add.
  - The tier-comparison card's exclusion line on /book said flatly "Not
    included: oven & fridge interiors, inside cabinets, baseboards" —
    now says "Not in the base price: ... — all addable next," since
    they're no longer hard exclusions.
  - /pricing's Move-Out section still described a large-home square-
    footage surcharge (+$100/$200/$300 tiers) and a "custom quote over
    3,400 sq ft" note — both leftover from before round 24 removed
    square footage from /book entirely. That page had never been
    updated to match; it now says what round 24 actually shipped (flat
    bedroom pricing, any size, no sq-ft lookup) and the worked example
    and JSON-LD price range (was still citing the old $899 ceiling from
    when surcharges existed) were updated to match the new $699 top
    price.
  - /pricing's add-ons ledger and Optional Add-ons table gained rows
    for Oven Interior, Inside Cabinets & Closets, and Detail Pass,
    matching what's actually purchasable now.


--------------------------------------------------------
5. SITE-WIDE FIX: THE "CALL" BUTTON COULD SILENTLY DO NOTHING
--------------------------------------------------------
This is the bug behind "call button is broken." It wasn't broken on
every tap — it was broken specifically when Google's ad-blocked or
slow-loading tracking script didn't get a chance to run, which on a
phone with any ad blocker, content blocker, or slow connection is not
rare.

The old code, on every page's tel: link:
  function gtag_report_conversion(url) {
    var callback = function () { window.location = url; };
    gtag('event', 'conversion', { ..., 'event_callback': callback });
    return false;
  }
`gtag()` is defined locally as a stub that just queues events — it does
nothing by itself. The REAL Google Ads script (loaded from
googletagmanager.com) is what eventually processes that queue and
calls `event_callback`. If that script is blocked, slow, or fails to
load for any reason, `event_callback` is never invoked — and since
`return false` also cancels the link's own default tel: action, tapping
Call just... did nothing. No error, no fallback, no phone call.

Fixed with a 500ms fallback timer that fires the navigation either way,
with a guard so it never double-fires if the real GA callback also
lands:
  function gtag_report_conversion(url) {
    var navigated = false;
    var go = function () { if (navigated) return; navigated = true;
      if (typeof(url) != 'undefined') window.location = url; };
    gtag('event', 'conversion', { ..., 'event_callback': go });
    setTimeout(go, 500);
    return false;
  }
Conversion tracking still fires normally on every call where Google's
script loads in time (the common case) — this only changes what
happens in the failure case, from "nothing" to "the call goes through,
slightly before or after the tracking pixel would have landed."

Applied to every page on the site that has a Call button or a tel:
link using this pattern — the header nav (FAQ / Call / Text us) present
site-wide, plus /book's calendar-screen "Call us" link and every
location/service landing page. 17 files total; each was carrying its
own independent copy of this script (not shared code), so each needed
the same fix individually. All 17 verified with node --check afterward.


--------------------------------------------------------
VERIFIED THIS ROUND
--------------------------------------------------------
node --check clean on the engine and every inline <script> in all 20
  touched files (JSON-LD blocks excluded from that check, same as
  always).
Pricing psychology re-verified at all four bedroom sizes: buying every
  Refresh add-on always costs $25-$45 more than switching to Inspection
  Ready outright (see section 2 above).
Grep-audited for stale references to the old three-separate-rows scope
  layout ("Wall spot-cleaning" as its own row, the old "oven and fridge
  interiors, inside cabinets, baseboards" exclusion phrasing used as a
  factual claim rather than a Liz sales line) — none found outside one
  intentionally-kept objection-script line in /call that's still
  accurate as general framing.
Confirmed rpAddonLineItems(), rpAddonsTotal(), and the webhook payload
  all pick up the three new add-ons through the same shared functions
  every other add-on uses — nothing added addon-specific code to the
  estimate screen, crew sheet, or /call's job summary.
Confirmed /call's own addon picker, "removable addon" price-objection
  logic, and scope cheat sheet all got the same three-add-on and
  real-price treatment as /book, so a phone booking and a self-serve
  booking can no longer quote different things for the same home.


--------------------------------------------------------
NOT price-verified in a live browser
--------------------------------------------------------
The actual click-through on a real phone, especially:
  - the three new Yes/No add-on cards on Refresh's add-ons screen
  - the combined Detail Pass row reading naturally alongside the two
    still-separate $50 rows (oven, cabinets) in the scope table
  - tapping Call on a phone with an ad blocker enabled, to confirm the
    fallback timer actually recovers the tap (hard to fully simulate
    without one)
Please walk a Refresh booking once end to end, and test the Call
button specifically from a phone if you have an ad blocker or privacy
browser installed, before calling this fully closed out.


--------------------------------------------------------
NOT DONE (by scope, not by accident)
--------------------------------------------------------
- No fridge+oven+cabinets bundle discount — kept as three flat $50
  add-ons rather than adding bundle-pricing UI complexity.
- No bathroom-count surcharge and no square-footage surcharge were
  reintroduced — both stay deliberately removed per round 24.
- Carpet, garage floor wash, exterior window, and junk hauling pricing
  are untouched this round — this round only touched the two Move-Out
  tier bases and the Refresh-side add-on catalog.
- The homepage's separate, older embedded booking widget (still out of
  sync since round 19c, flagged again every round since) is still
  untouched — still true that it's further out of sync than before,
  now including the new pricing.


--------------------------------------------------------
6. FOLLOW-UP (same round): NEW ADD-ONS WERE BURYING CARPET & WINDOWS
--------------------------------------------------------
Your note: "I don't like the move-out addons from the refresh toppling
over the high quality addons like carpet and windows. Can there be a
little box to check to add them in the comparison screen?"

Real bug, caused by section 2 above. The add-ons screen only shows the
first 3 cards by default (everything past that collapses behind a "Show
more" toggle) — before this round that top-3 slot was fridge, carpet,
windows for Refresh. Prepending oven, cabinets, and Detail Pass ahead of
carpet in the add-on list bumped BOTH carpet and windows out of that
visible slot and behind "Show more," which is backwards: those are the
bigger, more-considered purchases and shouldn't be the ones hidden.

Fixed exactly as you suggested: Refrigerator Interior, Oven Interior,
Inside Cabinets & Closets, and Detail Pass are no longer cards on
Refresh's add-ons screen at all. They're checkboxes on the moveouttiers
comparison screen instead, right below the scope table that already
prices each of them out — labeled "Going with Refresh? Add any of
these:". Same state fields, same toggle functions as before, so nothing
about pricing, the estimate, the webhook, or the crew sheet changed —
only where you check the box. Checking one and then choosing Inspection
Ready instead is harmless; the total only counts them on Refresh, since
Inspection Ready already includes all of it for free.

Net effect on Refresh's add-ons screen: only Carpet Cleaning, Exterior
Windows, and Garage Floor Wash remain, all three visible up front with
no "Show more" needed. This is /book only — /call's addon picker for
Liz never had this problem (it lists every available add-on as static
checkboxes with no top-3 collapse), so it was left as-is.

Verified with a scripted browser pass: the comparison screen renders
the four checkboxes correctly under the scope table, checking two of
them (fridge + oven) correctly adds $100 and follows through to the
add-ons screen's running total, and the add-ons screen itself now shows
carpet/windows/garage up front with nothing collapsed. node --check
clean on book/index.html afterward.


--------------------------------------------------------
7. AUDIT PASS: "did you check /book, /call and the FAQ" + prep Liz
--------------------------------------------------------
You asked me to scan the round for micro-optimizations, check whether
/book, /call, and the FAQ actually got the round's changes and any
recommended additions, and specifically prepare Liz to talk through the
tier difference on the phone. Here's what that turned up and what I
fixed.

CALL BUTTON, STILL BROKEN-LOOKING (separate bug from section 5 above)
You flagged "call button still goofy looking" with a screenshot: FAQ and
Text Us render as proper pill buttons, Call renders as bare text between
them. Cause: the shared button styling rule (background, border, color,
font-weight) only ever listed ".rp-head-faq, .rp-head-text" -- ".rp-head-
call" had its own separate rule for spacing/sizing but was never added
to the rule that actually makes it look like a button. Pre-existing
since round 23 (when Call was first added next to Text Us), unrelated to
this round's work otherwise. Fixed by adding .rp-head-call into that
shared rule and its :active state. /book only -- the location landing
pages use a different button system (.book-btn) that was never affected.

FAQ HAD NOTHING ABOUT THE TWO MOVE-OUT TIERS
The FAQ (reachable from every service, not just Move-Out) never
mentioned Inspection Ready vs. Refresh at all -- a real gap given it's
the site's core differentiated pricing decision. Added a new question,
"What's the difference between Inspection Ready and Move-Out Refresh?",
right after the first question, explaining both tiers, both guarantees,
and that individual items can be added to Refresh or bundled by
choosing Inspection Ready outright.

LIZ HAD A ONE-LINE HINT, NOT A CONVERSATION
Per your ask -- "prepare Liz to have conversations about the difference
... sells to both the customer who needs it perfect and the budget
people" -- the only guidance on /call was a single hint line per option
on the service picker, plus a short "say it as" line on the scope cheat
sheet. Neither gave her an actual two-sided script. Added:
  - A full "Watch" script on the service-picker step (the very first
    screen, before either tier is even picked): ask whether anyone's
    inspecting the home, then a real pitch for EACH answer -- Inspection
    Ready framed as what most people book to protect a deposit, Refresh
    framed as a genuine recommendation for a real situation, not a
    downsell. Ends by telling her the quote screen will flag it if the
    numbers say Inspection Ready is now the better deal (see next item).
  - Expanded the scope cheat sheet's closing line the same way.

INSPECTION READY CAN BECOME THE BETTER DEAL AND NOBODY SAID SO
This is the one I'd flag as the most consequential fix. The whole
add-on pricing in section 2 was deliberately built so stacking every
excluded-scope add-on on Refresh always costs MORE than switching to
Inspection Ready outright -- but nothing on either /call or /book ever
said this to anyone. A caller or self-serve customer could genuinely end
up paying more for Refresh-plus-everything than Inspection Ready would
have cost, for less scope and no deposit guarantee -- worse for them,
and it leaves money and a happier customer on the table for you. Fixed
on both sides, computed live off the real bedroom-tier prices so it
can't drift out of sync:
  - /call: a new guardrail on the addons and quote screens. Once the
    add-ons total reaches the tier-price gap, an info box appears with
    the exact numbers and a line to say out loud, next to the existing
    "Switch to Inspection Ready" button.
  - /book: the "Need it guaranteed for an inspection?" link on the
    estimate screen now checks the same math. Below the threshold it's
    unchanged; at or above it, it swaps to "With these add-ons you're at
    $X — Inspection Ready is $Y and fully guaranteed" with real numbers.
Tested at $150 in add-ons (below the $230 gap for a 3-bed home -- stays
quiet) and $275 (above it -- fires with the correct live numbers) on
both /call and /book.

ONE MORE STALE PRICE, ON A LANDING PAGE
Swept every location/resource page for old move-out price figures.
pcs-cleaning-fort-sill-ok/index.html had a "PCS Package" price list
still quoting the old $199-$599 range (last touched before round 25's
repricing) linked to a "?service=moveout" button -- i.e. it was quoting
old Inspection Ready numbers for the current Inspection Ready link.
Updated to $379 / $479-$589 / $699 by home size. Nothing else on any
other page referenced an old price.

SMALLER FIXES WHILE AUDITING
  - /pricing's <meta description> claimed "oven, fridge, cabinets and
    closets included -- not add-ons" as a blanket site-wide claim; it's
    only true on Inspection Ready now that Refresh sells them
    individually. Reworded to describe both tiers instead. (The visible
    on-page promise list already correctly said "on Inspection Ready" --
    only the meta tag was the blanket version.)

WAS THIS A GOOD IDEA / IS IT CLEAR -- my honest read
The underlying logic is sound: Inspection Ready needed the increase to
actually fund its own guarantee, Refresh stays the honest budget option,
and the a-la-carte-always-costs-more design protects both tiers from
cannibalizing each other. Two things worth watching, not fixing --
they're judgment calls, not bugs:
  - Inspection Ready moved up 21-27% in one shot. That's a real jump for
    anyone who saw the old number recently (a repeat customer, a referral
    who heard a friend's price). Worth watching booking volume on that
    tier for a few weeks -- if it drops noticeably, that's the market
    telling you something the cost math alone won't show.
  - The "Most Popular" badge sits on Inspection Ready unconditionally.
    That's presumably intentional (it's the better-margin tier), but it
    does mean the comparison isn't perfectly neutral between "sells to
    both" audiences -- flagging it so it's a choice you're making on
    purpose, not a default you inherited.
Not touched, flagged again as in every round since 19c: the homepage's
own separate, older embedded booking widget is now further out of sync
than ever -- it predates the tier split entirely and still shows old
junk pricing and a "yard" option that doesn't exist anymore. Given how
much has now diverged (two tiers, new add-ons, new prices), this is
probably worth a real look soon rather than another round of "still
flagging it."


--------------------------------------------------------
8. DEEP AUDIT: "make sure /book supports /call and the FAQs are good"
--------------------------------------------------------
You asked me to specifically check that /book and /call stay consistent
with each other and that the FAQ is accurate. This turned up the most
serious issue found in this whole round.

/CALL COULD QUOTE A DIFFERENT PRICE THAN /BOOK FOR THE SAME HOME
Root cause: round 24 dropped square footage from Move-Out pricing on
/book entirely -- flat bedroom-tier price, no large-home surcharge, by
direct instruction. /call's own flow was never updated to match. It
still had a "sqft" question in both moveout and moveoutrefresh, and
that answer was still feeding the (never-deleted) large-home surcharge
formula in the engine: +$100/$200/$300 on Inspection Ready, +$50/$100/
$150 on Refresh, for anything 2,200 sq ft or over -- or, for anything
over 3,400 sq ft, routing the job to a custom quote instead of a price
at all. A caller with a 2,600 sq ft 3-bedroom home would have been
quoted $200 more over the phone than the identical home would get
booking online, or told "we'll call you back" for a home /book prices
instantly. That's the kind of thing that costs trust the moment a
customer compares notes with a neighbor or checks the website after
hanging up. Fixed by removing "sqft" from both flows in /call, so
rpState.sqft stays unset on both channels and they compute the exact
same price. Verified: a 3-bed/2-bath Inspection Ready home now returns
$479 on both /call and /book with nothing else changed. If a large-home
surcharge is ever wanted back, it needs to go on /book first -- /call
should never be the one carrying pricing logic the website doesn't.

HANDYMAN-INTEREST LEAD CAPTURE DIDN'T EXIST ON THE PHONE AT ALL
/book has had a universal "Need Small Handyman Repairs?" checkbox on
every service's add-ons screen for a while -- quoted separately, not
priced, just a lead flag sent to the office. It never existed on /call
in any form: not in the state, not on the addons screen, not in the
booking payload. A caller who mentioned wanting a small repair done had
nowhere for Liz to record it -- that lead was simply lost, where a
self-serve customer's always reached the office. Added the field, the
checkbox (same wording and placement as /book, right after junk haul),
the crew-note line, and the `handyman_interest` field in the booking
payload, matching /book's field name exactly so nothing downstream
needs a separate case for phone-booked jobs.

FAQ ACCURACY PASS
Read all 9 questions against current site behavior line by line:
  - "Is the instant estimate final?" said the estimate is based on
    "size, condition, and options" -- condition-based pricing was
    removed from every service a while back (nothing is priced by a
    Standard/Heavy/Extreme tier anymore), so that line was describing a
    pricing lever that no longer exists. Removed "condition."
  - "What do the add-ons cover?" said "larger junk loads have separate
    options" -- round 24 replaced the half/full/oversized junk picker
    with one single "quoted separately" checkbox; there's no load-size
    option anymore, on either channel. Reworded to describe what's
    actually there.
  - "What if something is missed?" was generic ("we'll return to fix
    it") without naming either guarantee, right below a new question
    that names both by name -- reworded to name Defend Your Deposit and
    the Satisfaction Guarantee explicitly, and cite the 48-hour window
    that's already the real policy everywhere else on the site.
  - The footnote's service-area list ("Lawton, Fort Sill, Elgin, Cache")
    was missing Medicine Park and Duncan -- both are core service-area
    cities everywhere else on this same page (the hero eyebrow, the
    outside-service-area note), just not in the FAQ footnote. Fixed to
    match.

VERIFIED THIS PASS
node --check clean on call/index.html and book/index.html after every
edit. Scripted browser check: /call's moveout flow no longer contains
"sqft" and computes $479 for a 3-bed/2-bath Inspection Ready home
(matching /book exactly); the handyman checkbox renders on a non-move-
out service's addons screen (confirmed universal, not gated) and
toggles rpState.handymanInterest correctly with no console errors.

=============================================================================
9. FRESH-LOOK PASS: /CALL WAS STILL MAKING LIZ PICK A MOVE-OUT TIER BLIND
   (direct instruction: "check again think differently, how's the sales
   flow, what's counter-intuitive, what would work better, apply all
   recommended settings")
=============================================================================
THE FIND
Every previous round this session treated /book and /call as basically
symmetric and hunted for places they'd drifted apart. Looking at the
sales flow itself with fresh eyes instead of a consistency checklist
turned up something the consistency audits couldn't see, because both
channels weren't actually meant to work the same way here -- one of
them had simply never caught up to a decision the other made two
rounds ago.

Round 19c rebuilt /book specifically to stop making customers pick
Inspection Ready vs. Move-Out Refresh before a single real number
existed. The old flow asked "which tier?" as the very first question;
19c changed it to one "Move-Out Cleaning" entry, ask bedrooms and
bathrooms, THEN show both real prices side-by-side and let the
customer choose with actual numbers in front of them. That fix was
never brought over to /call. The CSR console still had "Inspection
Ready Move-Out" and "Move-Out Refresh" as two separate buttons on the
very first screen Liz sees -- she had to commit the caller to a tier
based only on one qualitative question ("will a landlord inspect
this?"), before bedrooms, bathrooms, or a single dollar figure existed.
The only way to correct a bad early call was a "switch tiers" link
buried on the quote screen, after the entire addons conversation had
already happened around the wrong number.

This is the real-world version of the exact problem 19c was written to
solve on the website, just still live on the phone: a caller says "not
sure, maybe?" to the landlord question, Liz picks a tier on a guess,
spends five minutes walking through add-ons priced against that guess,
and only then does anything correct it -- or doesn't, if she doesn't
think to use the switch link once she's already committed out loud.

WHAT WAS CHANGED (call/index.html)
  - "service" step now shows one "Move-Out Cleaning" entry instead of
    two separate tier buttons (rpServices itself is untouched -- both
    real keys still exist for every other screen, the shared engine,
    guardrails, and SCRIPT; only this one screen's rendering merges
    them). Picking it defaults rpState.service to "moveout" as a
    placeholder, same convention /book has used since 19c.
  - New "tiers" step, inserted into FLOWS.moveout and
    FLOWS.moveoutrefresh right after "questionnaire" and before
    "addons" -- the same position /book's "moveouttiers" occupies.
    Shows both real prices for the home just described
    (rpMoveoutTierBasePrice(), the same call /book's screen makes) and
    lets Liz pick -- or confirm -- the tier there, with the caller, out
    loud. Leaving it on the default (no explicit pick) still prices as
    Inspection Ready if she skips ahead, the safer of the two to fall
    back on.
  - New chooseMoveoutTier(k) sets the real service and advances --
    distinct from the existing switchMoveoutTier(k), which still does
    its original job: a later course-correction from the quote screen,
    after add-ons exist, with the already-selected-but-now-unavailable
    ones dropped and announced. Nothing about that later safety valve
    changed.
  - The landlord/inspector question stays on "service" (asked before
    bedrooms/bathrooms are even known, same as before) but its SCRIPT
    text was trimmed to just planting that qualitative question --  the
    actual per-tier dollar pitch moved to the new "tiers" step's script,
    since that's the point real numbers exist to say out loud. New
    SCRIPT.tiers entry states both real prices dynamically in its "say"
    line.
  - The tiers screen's fine-print also now reads the rental/selling
    answer already captured on "intent" and leads with Inspection Ready
    when they're selling -- the same signal /book's old moveouttiers
    screen used to read before round 24 removed that question from
    /book's own flow (call still asks it on its own script step, it
    just wasn't being used anywhere after round 24). This is the actual
    tier-decision screen now, so it's a more useful place for that nudge
    than the scope cheat sheet three screens later, after a tier is
    probably already picked.
  - rpMoveoutBlocked()'s "stop, don't quote a price" guardrail list
    gained "tiers" alongside the other pre-price steps, so a blocked
    home (water off, AC off, mold/pests) still can't reach a priced
    screen without the guard showing.
  - rpState gained moveoutTierChosen (mirrors /book's field of the same
    name), reset in pickService() same as every other move-out-specific
    field.

WHY NOT JUST LEAVE THE LATE "SWITCH TIERS" LINK
That link still exists and still works -- it's for a caller who changes
their mind mid-call after hearing the full add-on list, which is a real
and different scenario. It was never a substitute for putting real
numbers in front of the decision at the point the decision is actually
made. Relying on it as the only correction meant every call started
with a guess and needed an active save to fix, instead of the numbers
just being there before the guess had to happen.

VERIFIED THIS PASS
node --check clean on call/index.html. Scripted browser walkthrough,
local server + Playwright: confirmed the merged "Move-Out Cleaning"
service entry renders as one row; walked bedrooms=3/bathrooms=2 through
to the new "tiers" screen and confirmed it shows Inspection Ready $479
and Move-Out Refresh $249 (matching rpMoveoutTierBasePrice() and
/book's own numbers for the same size home); confirmed picking either
card sets rpState.service and advances straight to "addons" with that
service's correct add-on list (oven/cabinets/detailPass present for
Refresh); confirmed a 4-bedroom "selling" scenario shows $589/$299 and
the seller-specific lead-with-Inspection-Ready nudge; confirmed the
existing quote-screen switchMoveoutTier() safety valve still works
unchanged after the new screen is added ahead of it in the flow.


Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_012kaXXZT6YqpFLG2HXv3Xub
