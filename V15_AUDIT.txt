Red Prairie Cleaning — v15 audit

Checks completed
- 20 HTML files parsed.
- All executable inline JavaScript blocks passed node --check.
- All local href/src references resolved to files or valid site routes.
- No duplicate HTML IDs remain.
- All 18 legacy homepage/landing/content pages contain the new theme and /book wordmark.
- All 18 legacy pages completed a headless mobile render smoke test with no page-level JavaScript errors.
- /book add-on flow was tested on a 390x844 mobile viewport: add carpet, reopen it, remove it, and verify the selected state and price clear.

Notable items left intentionally unchanged
1. The old embedded estimator remains hidden inside 15 legacy pages. Those pages are roughly 146–152 KB each and still carry duplicated estimator CSS/JS. Once /book has proven stable with real traffic, removing that dead payload is the clearest performance cleanup.
2. Hidden legacy estimator pricing is stale in places (for example garage wash still shows $100 there, while /book correctly uses $150). It is not customer-visible while the old estimator remains hidden, but it is another reason not to re-enable it without updating it first.
3. /book currently treats bundled carpet cleaning as $50 per room while standalone carpet cleaning is $75 per room with a two-room minimum. The code describes the $50 price as an intentional bundle rate; confirm that this is still the desired pricing policy.
4. Refrigerator interior remains a $50 add-on.
5. Review proof is hard-coded as 37+ across the site. Update it manually when the public count materially changes.
6. House-cleaning pages still route to maintenance and the PCS page routes to move-out, matching v14's existing mapping.
