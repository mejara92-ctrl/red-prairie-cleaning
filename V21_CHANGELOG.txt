RED PRAIRIE CLEANING — V21 CHANGELOG

Pricing integrity fix (customer-facing bug)
- Garage Floor Wash was priced at $150 in /book but $100 in all 14 other
  pages running the legacy inline estimator (homepage + 13 landing pages).
  Corrected all 14 to $150 to match /book. This was flagged as a stale-price
  risk in the V15 audit and had not been fixed until now.
- Verified this was the ONLY pricing discrepancy: every other add-on price,
  base service tier, and pricing formula already matched /book exactly
  across all pages.

Consistency safeguard
- Deployed js/rp-canonical-estimator.js to all 13 legacy landing pages that
  were missing it (previously only referenced in a comment on the
  homepage, not actually loaded there either). This script standardizes
  checklist copy, calendar URL, and other shared behavior across pages so
  future edits to /book don't silently drift out of sync on legacy pages
  the way the garage price did.
- Ran via the existing tools/apply-canonical-estimator.py (already in the
  repo, previously unexecuted).

Asset cleanup
- Removed images/team/hero-cleaning-team.jpg and .webp — byte-identical
  duplicates of images/hero/hero-cleaning-team.{jpg,webp}, unreferenced by
  any page.
- Removed images/team/test.txt, a stray 1-byte leftover file.

Verification
- node --check passed on every inline <script> block (JS, not JSON-LD)
  across all 17 pages after edits.
- Confirmed zero remaining "$100" garage-wash references anywhere in the repo.
- Confirmed zero duplicate canonical-estimator script tags.
- No visual, structural, or pricing-formula changes made to /book itself.

Not done in this pass (flagged, not fixed — needs your call)
1. The 13 legacy landing pages (+ homepage) still carry their own ~46KB
   duplicated inline estimator (the dead code the canonical script patches
   over). Page weight is unchanged (still ~148–152KB) because removing that
   inline block means either rebuilding each page's estimator UI or
   redirecting those pages' estimator CTAs straight to /book — a real
   scope decision, not a safe automatic edit. Recommend discussing this as
   its own project.
2. Refrigerator Interior remains at $50 with the original "not explicitly
   specified in the brief" flag still in the code comment — needs your
   confirmation, not a code fix.
3. Bundled carpet ($50/room via /book) vs. standalone carpet ($75/room,
   2-room minimum) is still just documented as intentional — needs your
   sign-off if it hasn't already happened elsewhere.
